icon_sizes = [
  'scalable',
  'symbolic'
]

foreach icon_size: icon_sizes
  install_subdir(
    'hicolor/' + icon_size,
    install_dir: join_paths(gnome_todo_datadir, 'icons', 'hicolor')
  )
endforeach
